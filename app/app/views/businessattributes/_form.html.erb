<%= form_for(@businessattribute) do |f| %>
  <% if @businessattribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@businessattribute.errors.count, "error") %> prohibited this businessattribute from being saved:</h2>

      <ul>
      <% @businessattribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_id %><br />
    <%= f.text_area :business_id %>
  </div>
  <div class="field">
    <%= f.label :attribute_id %><br />
    <%= f.number_field :attribute_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
